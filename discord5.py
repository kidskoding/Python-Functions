#we can use the return key to allow the terminal to return a value to us

def function(x): #defined function as x
  return 5 + x #this is what is inside the function so the terminal will return 5 + x. x is 3 so terminal returns 8
print(function(3)) #3 substitutes as x because x = 3
